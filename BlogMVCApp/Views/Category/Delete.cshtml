@model BlogMVCApp.Models.Category

@{
    ViewBag.Title = "Delete";
}

<h2 class="text-muted text-center">Kategori Silme Ekranı</h2>
<hr />
<h3 class="text-muted">Bu Kategoriyi Silmek İstediğinize Emin Misiniz?</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.KategoriAdi)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.KategoriAdi)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <button type="submit" class="btn btn-danger btn-sm"><i class="fa fa-trash"></i> Kategori Sil</button>
            <a href="@Url.Action("Index")" class="btn btn-secondary btn-sm"><i class="fa fa-hand-o-left"></i>Listeye Geri Dön</a>

        </div>
    }
</div>
